export const addPetition = async() => {
    try {
        
    } catch (error) {
        return res.status(500).json({ message:error.message });
    }
}

export const getPendientPetition = async() => {
    try {
        
    } catch (error) {
        return res.status(500).json({ message:error.message });
    }
}